services:
  - type: web
    name: ghostpro-api
    runtime: python
    buildCommand: ""
    startCommand: uvicorn GhostPROAPI:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
